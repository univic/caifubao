# Caifubao 项目操作参考

## 与 Agent 规则体系的关系

本文件是**项目操作参考**（开发流程、Git 规范、OpenSpec 命令、FAQ）。
Agent 行为规则（安全、模块边界、外科纪律、验证）定义在 [`RULES.md`](./RULES.md) 中。
Agent 角色写范围定义在 `.opencode/agents/*.md` 中。

**规则优先级**: `RULES.md` > 本文件。当本文件中的流程描述与 `RULES.md` 规则冲突时，以 `RULES.md` 为准。

---

## 项目概述

Caifubao 是一个大 A 量化分析系统，包含前端（Vue 3）和后端（Python Flask）。

## 技术栈与项目结构

```
caifubao/
├── backend/          # Python 3.12+ Flask 后端 (MongoDB + MongoEngine)
├── frontend/         # Vue 3 + TypeScript + Vite + Element Plus + Pinia
├── datahub/          # 数据采集、因子计算、评分引擎、数据质量
├── k8s/              # Kubernetes 部署配置
└── openspec/         # OpenSpec 开发规范和变更管理
```

- **后端端口**: 8000 (虚拟环境: `backend/venv312`)
- **前端端口**: 5173 (构建输出: `frontend/dist/`)

## 代码风格与安全

定义在 `RULES.md`：
- 安全规则: [`RULES.md#p1--safety`](./RULES.md#p1--safety-must-not-violate)
- 外科纪律: [`RULES.md#p4--surgical-discipline`](./RULES.md#p4--surgical-discipline-karpathy-principles-apply-to-all-agents)
- 代码模式: [`RULES.md#p6--existing-patterns`](./RULES.md#p6--existing-patterns-prefer-but-lower-priority-than-above)

模块级开发规则定义在对应 Agent 文件中:
- 后端: [`.opencode/agents/backend-implementer.md`](./.opencode/agents/backend-implementer.md)
- 数据: [`.opencode/agents/datahub-implementer.md`](./.opencode/agents/datahub-implementer.md)
- 前端: [`.opencode/agents/frontend-implementer.md`](./.opencode/agents/frontend-implementer.md)
- 部署: [`.opencode/agents/k8s-implementer.md`](./.opencode/agents/k8s-implementer.md)

## 提交规范

使用 Conventional Commits: `feat:`, `fix:`, `docs:`, `style:`, `refactor:`, `test:`, `chore:`.

完整定义: [`RULES.md#p6--existing-patterns`](./RULES.md#p6--existing-patterns-prefer-but-lower-priority-than-above)

---

## OpenSpec 开发规范

### 工作流程

1. **创建变更**：使用 `/opsx:new` 创建新的变更
2. **编写规范**：在 `openspec/changes/<change-name>/` 目录下编写 artifacts
3. **应用变更**：使用 `/opsx:apply` 实现任务
4. **继续变更**：使用 `/opsx:continue` 继续未完成的变更
5. **归档变更**：使用 `/opsx:archive` 归档已完成的变更

### Artifacts（按优先级）

1. **proposal.md** — 变更提案（Why / What Changes / Capabilities / Impact / 非目标）
2. **specs/** — 规格说明（SHALL 语法 + Scenario WHEN-THEN 格式）
3. **design.md** — 设计文档（架构、技术选型、数据流）
4. **tasks.md** — 任务列表（checkbox 标记：`- [ ]` 未完成，`- [x]` 已完成）

### 通用命令

```bash
# 状态检查
openspec status --change "<name>" --json

# 获取应用指令
openspec instructions apply --change "<name>" --json
```

### Spec Gate 触发条件

定义在 [`RULES.md#p3--spec-gate`](./RULES.md#p3--spec-gate-required-when-any-of-these-change)

### 最佳实践

1. **先写规范，再写代码**
2. **保持 artifacts 同步** — 实现过程中发现问题及时更新 design.md
3. **任务分解** — 每个任务 2 小时内完成，按逻辑顺序排列
4. **变更管理** — 每个变更专注一个功能/模块，完成后及时归档

**配置文件**: `openspec/config.yaml` · Schema: `spec-driven`

---

## 本地开发

```bash
# 后端
cd backend && source venv312/bin/activate && python app/run.py

# 前端
cd frontend && npm run dev
```

确保 MongoDB 服务运行。

## 构建

```bash
# 后端
docker build -t caifubao-backend .

# 前端
cd frontend && npm run build
```

## 部署

```bash
# 开发环境
./k8s/deploy.sh development -s --ingress

# 生产环境
./k8s/deploy.sh production -s --ingress
```

---

## Git 工作流规范

### 分支策略

本项目采用 **Git Flow** 工作流，主要分支：
- `main` — 生产环境代码，只能接受来自 `develop` 或 hotfix 的合并
- `develop` — 开发环境代码，所有功能分支的默认合并目标
- `feature/*` — 功能分支，从 `develop` 创建
- `fix/*` — 修复分支，从 `develop` 创建
- `hotfix/*` — 热修复分支，从 `main` 创建（仅用于紧急修复）

### 代码变更流程（强制规则）

**⚠️ 重要：任何代码变更必须遵循以下流程**

#### 1. 新建分支（强制）

**每个独立的非紧急任务必须从 `develop` 新建独立分支。禁止在已有分支上混入无关改动。**

```bash
# 获取最新 develop
git fetch origin develop --quiet

# 基于 develop 创建新分支
git checkout -b feature/<功能名> origin/develop

# 或修复分支
git checkout -b fix/<问题描述> origin/develop
```

**例外**：仅当 orchestrator 在一个 session 内继续同一任务时，可以复用当前分支。

**分支命名规范**：
- `feature/<功能名>` — 新功能开发
- `fix/<问题描述>` — Bug 修复
- `refactor/<描述>` — 代码重构
- `docs/<描述>` — 文档更新
- `chore/<描述>` — 构建/工具相关

#### 2. 开发工作

- 遵循 `RULES.md` 规则和 OpenSpec 工作流程
- 遵循对应 Agent 的开发规则
- 编写必要的测试
- 保持提交历史清晰

#### 3. 提交代码

```bash
git add .
git commit -m "feat: 添加市场总览 Dashboard 功能

- 实现实时行情展示
- 添加 K 线图组件
- 集成 WebSocket 数据推送"

git push -u origin feature/market-dashboard
```

#### 4. 质量审查（强制）

创建 PR 前，orchestrator 必须调用 reviewer 子 agent：

- `contract-reviewer`：API 合约/鉴权/freshness/OpenClaw 变更时
- `qa-reviewer`：所有非 trivial 代码变更时

P1 问题必须修复后重新 review。详见 `RULES.md#review-gates`。

#### 5. 检查冲突（强制）

```bash
# 获取最新的 develop 分支
git fetch origin develop --quiet

# 检查是否有冲突
git merge-tree $(git merge-base HEAD origin/develop) origin/develop HEAD
```

**如果有冲突**：解决所有冲突 → 重新验证 → 提交。
**如果无冲突**：继续下一步。

#### 6. 创建 Draft PR（强制）

**所有 PR 必须以 Draft 状态创建。** 这意味着代码正在等待 CI 验证，尚未准备好人工 review。

```bash
gh pr create --draft --base develop --title "feat: 添加市场总览 Dashboard 功能" --body "## 概述
简要描述这个 PR 的目的。

## 变更内容
- 功能 1
- 功能 2

## 测试
- [ ] 单元测试通过
- [ ] 集成测试通过
- [ ] 手动测试通过

## 检查清单
- [ ] 代码遵循 RULES.md 规范
- [ ] 已解决与 develop 的冲突
- [ ] 文档已更新
- [ ] 无敏感信息泄露"
```

**PR 规范**：标题使用 Conventional Commits · 目标分支必须是 `develop` · **必须以 `--draft` 创建**

#### 7. 等待 CI 通过（强制）

创建 Draft PR 后，等待 GitHub Actions 完成。**必须在 CI 全部通过后才能转为正式 PR。**

1. 确认所有 CI jobs 状态为 ✅（绿色）
2. 如果有任何 job 失败（❌），修复代码并重新 push，回到步骤 1
3. 如果有 job 仍在运行（🟡），等待完成

#### 8. 转为正式 PR（强制）

CI 全部通过后，将 Draft PR 转为正式 PR：

```bash
gh pr ready <PR_NUMBER>
```

**禁止在 CI 未通过或仍在运行时转换 Draft PR。**

#### 9. 合并 PR

使用 **Squash and merge**，删除功能分支。

### 冲突解决

**谁负责**: 创建 PR 的人负责解决冲突。

**预防**: 经常从 develop 拉取更新，小步快跑，频繁提交 PR。

### 紧急修复（Hotfix）

```bash
git checkout main
git checkout -b hotfix/critical-bug
# ... 修复 ...
# 需要两个 PR：
# 1. hotfix -> main（紧急部署）
# 2. hotfix -> develop（同步到开发分支）
```

### 禁止事项

❌ 直接向 `main` 或 `develop` 推送
❌ 绕过 PR 流程合并
❌ CI 失败情况下强制合并
❌ 不解决冲突就创建 PR
❌ `--force` 推送到共享分支
❌ 在别人的分支上混入无关改动
❌ 在 `develop` 或 `main` 上直接编辑

---

## 常见问题

### 后端
- 虚拟环境路径：`backend/venv312`
- Python 版本：3.12.12
- 端口：8000

### 前端
- 开发服务器端口：5173
- 构建输出：`frontend/dist/`

### OpenSpec
- 配置文件：`openspec/config.yaml`
- 变更目录：`openspec/changes/`
- Schema：`spec-driven`
