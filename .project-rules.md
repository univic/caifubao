# Caifubao 项目通用规则

## 项目概述

Caifubao 是一个大A量化分析系统，包含前端（Vue 3）和后端（Python Flask）。

## 项目结构

```
caifubao/
├── backend/          # Python Flask 后端
├── frontend/         # Vue 3 前端
├── k8s/              # Kubernetes 部署配置
└── openspec/         # OpenSpec 开发规范和变更管理
```

## 项目架构

### 技术栈
- **前端**: Vue 3 + TypeScript + Vite + Element Plus + Pinia + Vue Router + ECharts
- **后端**: Python 3.12 + Flask + MongoDB + MongoEngine + APScheduler
- **部署**: Kubernetes + Docker + Traefik

### 开发原则
1. 所有功能变更必须遵循 OpenSpec 工作流程
2. 先写规范，再写代码
3. 保持代码简洁和可维护性
4. 遵循各语言的最佳实践

## OpenSpec 开发规范

### OpenSpec 工作流程

1. **创建变更**：使用 `/opsx:new` 创建新的变更
2. **编写规范**：在 `openspec/changes/<change-name>/` 目录下编写 artifacts
3. **应用变更**：使用 `/opsx:apply` 实现任务
4. **继续变更**：使用 `/opsx:continue` 继续未完成的变更
5. **归档变更**：使用 `/opsx:archive` 归档已完成的变更

### OpenSpec Artifacts

每个变更包含以下 artifacts（按优先级排序）：

1. **proposal.md** - 变更提案
   - 说明变更的目的（Why）
   - 描述变更内容（What Changes）
   - 列出新增和修改的能力（Capabilities）
   - 说明影响范围（Impact）
   - 明确非目标（非目标）

2. **specs/** - 规格说明目录
   - 每个功能模块一个 `spec.md` 文件
   - 使用 SHALL 语法定义需求
   - 使用 Scenario 格式描述场景
   - WHEN-THEN 格式描述行为

3. **design.md** - 设计文档
   - 架构设计
   - 技术选型
   - 数据流设计
   - 接口设计

4. **tasks.md** - 任务列表
   - 将需求分解为可执行的任务
   - 每个任务使用 checkbox 标记状态
   - `- [ ]` 未完成
   - `- [x]` 已完成

### Spec-Driven Schema

项目使用 `spec-driven` schema，工作流程如下：

1. **状态检查**：
   ```bash
   openspec status --change "<name>" --json
   ```

2. **获取应用指令**：
   ```bash
   openspec instructions apply --change "<name>" --json
   ```

3. **读取上下文文件**：
   - proposal.md
   - specs/*.md
   - design.md
   - tasks.md

4. **实现任务**：
   - 按顺序实现 tasks.md 中的任务
   - 每完成一个任务，更新 checkbox
   - 保持变更最小化

### 需求规范格式

#### SHALL 语法
使用 SHALL 语法定义需求：

```markdown
### Requirement: <需求名称>
The system SHALL <系统应该做什么>
```

#### Scenario 格式
使用 Scenario 格式描述场景：

```markdown
#### Scenario: <场景名称>
- **WHEN** <触发条件>
- **THEN** <预期结果>
```

### OpenSpec 最佳实践

1. **先写规范，再写代码**
   - 在实现功能之前，必须先编写完整的 specs
   - 使用 SHALL 语法明确定义需求
   - 使用 Scenario 格式描述用户场景

2. **保持 artifacts 同步**
   - 如果实现过程中发现设计问题，及时更新 design.md
   - 如果需求变更，更新 proposal.md 和 specs
   - 保持 tasks.md 与实际进度同步

3. **任务分解**
   - 将大任务分解为小任务（每个任务 2 小时内完成）
   - 任务之间保持独立性
   - 按逻辑顺序排列任务

4. **变更管理**
   - 每个变更专注于一个功能或模块
   - 避免在同一个变更中混合不相关的功能
   - 完成后及时归档

5. **文档质量**
   - proposal.md 清晰说明变更的目的和影响
   - specs.md 使用标准格式，避免歧义
   - design.md 提供足够的技术细节
   - tasks.md 任务描述具体可执行

## 代码风格

### 通用规则
- 使用有意义的变量和函数命名
- 添加必要的注释和文档
- 保持代码简洁和可维护性
- 遵循各语言的最佳实践

### Python 规范
- 遵循 PEP 8 编码规范
- 使用类型注解
- 编写单元测试

### JavaScript/TypeScript 规范
- 使用 ESLint + Prettier
- 遵循 Vue 3 最佳实践
- 使用 TypeScript 严格模式

## 提交规范

使用 Conventional Commits 格式：

- `feat:` 新功能
- `fix:` 修复 bug
- `docs:` 文档更新
- `style:` 代码格式调整
- `refactor:` 重构
- `test:` 测试相关
- `chore:` 构建/工具相关

## 安全规则

### 环境变量
- 敏感信息（数据库密码、API 密钥等）必须使用环境变量
- 不要在代码中硬编码敏感信息
- 使用 `.env.example` 作为模板

### 其他安全规则
- 不要提交敏感信息到代码仓库
- 使用 `.gitignore` 排除不必要的文件
- 遵循安全最佳实践

## 后端开发规则

### Python 规范
- 使用 Python 3.12+
- 遵循 PEP 8 编码规范
- 使用类型注解
- 编写单元测试

### 项目结构
- `app/api/` - API 接口
- `app/conf/` - 配置文件
- `app/lib/` - 核心库（datahub、web_server、task_controller 等）
- `app/model/` - 数据模型（MongoEngine）
- `app/utilities/` - 工具函数

### 数据库
- 使用 MongoDB 作为主数据库
- 使用 MongoEngine 进行 ORM 操作
- 数据模型定义在 `app/model/` 目录

### 定时任务
- 使用 APScheduler 进行任务调度
- 定时任务配置在 `app/lib/datahub/`
- 任务持久化到 MongoDB

### Docker 部署
- 使用多阶段构建优化镜像大小
- 镜像大小应保持在 650MB 以下
- 使用 `.dockerignore` 排除不必要的文件

## 前端开发规则

### 技术栈
- Vue 3 (Composition API)
- TypeScript
- Vite
- Element Plus
- Pinia (状态管理)
- Vue Router (路由)
- Axios (HTTP 客户端)
- ECharts (图表)

### 项目结构
- `src/api/` - API 接口封装
- `src/components/` - 可复用组件
- `src/views/` - 页面组件
- `src/router/` - 路由配置
- `src/stores/` - Pinia 状态管理
- `src/styles/` - 全局样式

### 代码规范
- 使用 TypeScript 严格模式
- 组件使用 `<script setup>` 语法
- 使用 Composition API
- 遵循 Vue 3 最佳实践

### API 调用
- 统一使用 `src/api/` 目录下的接口封装
- 使用 Axios 拦截器处理请求和响应
- 统一错误处理

### 状态管理
- 使用 Pinia 进行状态管理
- Store 定义在 `src/stores/` 目录
- 避免过度使用全局状态

## Kubernetes 部署规则

### 项目结构
- `k8s/base/` - 基础配置
- `k8s/overlays/development/` - 开发环境配置
- `k8s/overlays/production/` - 生产环境配置
- `k8s/deploy.sh` - 部署脚本
- `k8s/cleanup.sh` - 清理脚本

### 部署原则
- 使用 Kustomize 管理多环境配置
- 使用 ConfigMap 和 Secret 管理配置和敏感信息
- 使用 Ingress 暴露服务
- 使用 PersistentVolume 持久化数据

### 部署脚本
- `deploy.sh` 支持命令行参数和静默模式
- `cleanup.sh` 支持清理资源

## 开发流程

### 本地开发
1. 后端：使用 `dev-entrypoint.sh` 启动
2. 前端：使用 `npm run dev` 启动
3. 确保 MongoDB 服务运行

### 测试
- 后端：运行单元测试 `pytest`
- 前端：运行单元测试 `npm test`

### 构建
- 后端：`docker build -t caifubao-backend .`
- 前端：`npm run build`

### 部署
- 开发环境：`./k8s/deploy.sh development -s --ingress`
- 生产环境：`./k8s/deploy.sh production -s --ingress`

## Git 工作流规范

### 分支策略

本项目采用 **Git Flow** 工作流，主要分支：
- `master` - 生产环境代码，只能接受来自 `develop` 的合并
- `develop` - 开发环境代码，所有功能分支的合并目标
- `feature/*` - 功能分支，从 `develop` 创建
- `fix/*` - 修复分支，从 `develop` 创建
- `hotfix/*` - 热修复分支，从 `master` 创建（仅用于紧急修复）

### 代码变更流程（强制规则）

**⚠️ 重要：任何代码变更必须遵循以下流程**

#### 1. 创建分支

如果当前没有恰当的分支，**必须从 `develop` 分支拉取新分支**：

```bash
# 确保本地 develop 分支是最新的
git checkout develop
git pull origin develop

# 创建新分支（根据变更类型选择前缀）
# 功能开发：feature/<功能名>
git checkout -b feature/market-dashboard

# Bug 修复：fix/<问题描述>
git checkout -b fix/user-auth-bug

# Kubernetes 配置修复：fix/k8s-<描述>
git checkout -b fix/k8s-deployment-config

# 应用代码修复：fix/app-<描述>
git checkout -b fix/app-code-issues
```

**分支命名规范**：
- `feature/<功能名>` - 新功能开发
- `fix/<问题描述>` - Bug 修复
- `refactor/<描述>` - 代码重构
- `docs/<描述>` - 文档更新
- `chore/<描述>` - 构建/工具相关

#### 2. 开发工作

在新分支上进行开发：
- 遵循 OpenSpec 工作流程（如果适用）
- 遵循代码风格规范
- 编写必要的测试
- 保持提交历史清晰

#### 3. 提交代码

```bash
# 添加变更
git add .

# 提交（遵循提交规范）
git commit -m "feat: 添加市场总览 Dashboard 功能

- 实现实时行情展示
- 添加 K 线图组件
- 集成 WebSocket 数据推送"

# 推送到远程
git push -u origin feature/market-dashboard
```

#### 4. 检查冲突（强制）

**工作完成后，必须检查与 `develop` 分支是否有冲突**：

```bash
# 获取最新的 develop 分支
git fetch origin develop

# 检查是否有冲突（不实际合并）
git merge-tree $(git merge-base HEAD origin/develop) origin/develop HEAD

# 或者创建临时分支检查
git checkout -b temp-check-conflict
git merge origin/develop --no-commit --no-ff
git status
```

**如果有冲突**：
1. 解决所有冲突文件
2. 测试确保功能正常
3. 提交解决后的代码
4. 删除临时检查分支

```bash
# 解决冲突后
git add .
git commit -m "fix: 解决与 develop 分支的合并冲突"
git checkout feature/market-dashboard
git merge temp-check-conflict
git branch -D temp-check-conflict
```

**如果无冲突**：
```bash
git merge --abort  # 如果在临时分支
git checkout feature/market-dashboard
git branch -D temp-check-conflict
```

#### 5. 创建 PR（强制）

**必须创建合并到 `develop` 分支的 PR**：

```bash
# 使用 GitHub CLI 创建 PR
gh pr create --base develop --title "feat: 添加市场总览 Dashboard 功能" --body "## 概述
简要描述这个 PR 的目的。

## 变更内容
- 功能 1
- 功能 2
- 修复 1

## 测试
- [ ] 单元测试通过
- [ ] 集成测试通过
- [ ] 手动测试通过

## 检查清单
- [ ] 代码遵循项目规范
- [ ] 已解决与 develop 的冲突
- [ ] 文档已更新
- [ ] 无敏感信息泄露"
```

**PR 规范**：
- **标题**：使用 Conventional Commits 格式（feat:/fix:/refactor: 等）
- **描述**：包含概述、变更内容、测试情况
- **审查人**：至少 1 人审查（核心功能需要 2 人）
- **目标分支**：必须是 `develop`

#### 6. 检查 GitHub Action（强制）

**创建 PR 后，必须检查 GitHub Action 是否通过**：

1. **查看 Action 状态**：
   - 访问 PR 页面
   - 查看 Checks 标签
   - 确认所有检查项通过 ✅

2. **Action 检查项通常包括**：
   - 代码风格检查（Lint）
   - 单元测试
   - 集成测试
   - 构建测试
   - 安全扫描

3. **如果 Action 失败**：
   ```bash
   # 查看失败日志
   gh run view <run-id> --log
   
   # 修复问题
   # ... 修改代码 ...
   
   # 重新提交
   git add .
   git commit -m "fix: 修复 CI 检查问题"
   git push
   ```
   
   **注意**：必须修复所有 Action 失败才能合并 PR

4. **Action 通过后**：
   - 请求审查人审查
   - 处理审查意见
   - 获得批准后合并

#### 7. 合并 PR

```bash
# 使用 GitHub CLI 合并
gh pr merge <pr-number> --squash --delete-branch

# 或者使用 GitHub Web 界面点击 "Squash and merge"
```

**合并策略**：
- 使用 **Squash and merge** 保持历史清晰
- 删除功能分支（已合并到 develop）

### 冲突解决策略

#### 谁负责解决冲突？
- **创建 PR 的人**负责解决冲突
- 如果冲突复杂，可以请求团队成员协助

#### 冲突解决步骤
1. 拉取最新的 develop 分支
2. 在本地合并 develop 到功能分支
3. 解决所有冲突文件
4. 测试确保功能正常
5. 提交并推送

#### 冲突预防
- 经常从 develop 拉取更新（`git pull origin develop`）
- 避免长时间不合并的功能分支
- 小步快跑，频繁提交 PR

### 紧急修复流程（Hotfix）

对于生产环境的紧急问题：

```bash
# 从 master 创建 hotfix 分支
git checkout master
git pull origin master
git checkout -b hotfix/critical-bug

# 修复问题并提交
# ...

# 创建 PR 到 master 和 develop
git push -u origin hotfix/critical-bug

# 需要两个 PR：
# 1. hotfix/critical-bug -> master（紧急部署）
# 2. hotfix/critical-bug -> develop（同步到开发分支）
```

### 禁止事项

❌ **以下行为严格禁止**：

1. 直接向 `master` 或 `develop` 推送代码
2. 在功能分支上直接合并其他功能分支
3. 绕过 PR 流程直接合并
4. 在 Action 失败的情况下强制合并
5. 不解决冲突就创建 PR
6. 使用 `--force` 强制推送到共享分支

### 常见问题

#### Q: 如果 develop 分支更新了，我需要重新检查冲突吗？
A: 是的，如果 develop 在创建 PR 后有更新，需要重新检查并解决冲突。

#### Q: 冲突太复杂，可以请求帮助吗？
A: 可以，在 PR 描述中说明冲突情况，@ 相关团队成员协助。

#### Q: Action 失败但不是我的代码问题？
A: 先确认失败原因，如果是环境问题，重试 Action；如果是代码问题，必须修复。

#### Q: 可以合并到 master 吗？
A: 不可以，只有 develop 分支可以合并到 master（通过发布流程）。

## 常见问题

### 后端
- 虚拟环境路径：`backend/venv312`
- Python 版本：3.12.12
- 端口：8000

### 前端
- 开发服务器端口：5173
- 构建输出：`dist/`

### OpenSpec
- 配置文件：`openspec/config.yaml`
- 变更目录：`openspec/changes/`
- Schema：`spec-driven`

## 注意事项

- 不要提交敏感信息到代码仓库
- 使用 `.gitignore` 排除不必要的文件
- 定期更新依赖包
- 遵循安全最佳实践
- **所有功能变更必须遵循 OpenSpec 工作流程**
- **先写规范，再写代码**
- **保持 artifacts 与代码同步**