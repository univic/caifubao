schema: spec-driven

# Project context (optional)
# This is shown to AI when creating artifacts.
# Add your tech stack, conventions, style guides, domain knowledge, etc.
context: |
  Tech stack: Python Flask backend, datahub jobs, Vue 3/Vite/Pinia/Element Plus frontend, MongoDB, k8s, GitHub Actions
  项目: 财经数据量化分析平台 (财富宝 caifubao)
  模块边界: datahub 生产行情/因子/质量数据，backend 提供聚合 API，frontend 消费 API 并展示
  状态: MVP 与研究阶段均已实现——登录、市场总览、历史行情、数据质量、信号、回测、多周期评分（Score5/20/60）、策略发现、网格搜索、因子评估、walk-forward、决策面板、OpenClaw 只读集成（service token 鉴权、只读数据 API、请求审计）
  数据链路 (2026-08): 行情/股票列表来源可切 tushare（pro.daily / pro.stock_basic，env 切换，默认 akshare）；每日增量走快照驱动 UPD 路径（pro.daily(trade_date) 结算快照，非逐只拉历史）；FQ 因子改用真实 tushare adj_factor（close_hfq = close × adj_factor）；评分推荐为横截面百分位驱动（BUY=top 5% / WATCH=top 20% / AVOID=bottom 20%），全市场评分可用 DATAHUB_SCORING_MODE=ranked 走组件 rank 归一化
  未完成: fq-adj-factor-fix 与 scoring-percentile-rank 两个 change 的部署/全市场验证任务未执行（见各 change tasks.md 5.x）；评分有效性全市场验收（12d.5 / 20.x）未做，carry-forward 见 GitHub issue #125
  已归档: openspec/archive/mvp-quant-demo/ (MVP 契约账本，含 11 份 spec)、openspec/archive/frontend/ (Django/Celery/AkQuant 旧架构，仅历史参考)、openspec/archive/homepage-20260321/ (首页提案，未实施)
  运营: 使用 ./scripts/caifubao 统一 CLI；agent 规则见 RULES.md / AGENTS.md；领域 skill 见 skills/
  性能盘点 (2026-08-28): 全量性能/CPU 热点审查结论与分阶段修复路线见 docs/operations/perf-analysis-2026-08.md；修复任务按 openspec/changes/datahub-perf-optimization/ 推进（P0：sync 无水位线全量重灌、signal 无增量全历史重算、scoring 逐股 N+1 与 prod 评分双跑、FQ 回填逐股 pacing 阻塞待办 5.2）
  注意: 仅在触及 API 契约、鉴权、评分语义、数据所有权或公开文档时才需要新建 OpenSpec change

# Per-artifact rules (optional)
# Add custom rules for specific artifacts.
# Example:
#   rules:
#     proposal:
#       - Keep proposals under 500 words
#       - Always include a "Non-goals" section
#     tasks:
#       - Break tasks into chunks of max 2 hours
